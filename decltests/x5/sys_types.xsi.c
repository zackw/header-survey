/* XSI features */
#include <sys/types.h>

/* We do not attempt to validate any of the constraints on what the
   types are defined to; we only confirm that all are complete. */

clock_t               ac;
fsblkcnt_t            af;
fsfilcnt_t            ag;
id_t                  ai;
key_t                 ak;
suseconds_t           bb;
/* useconds_t silently removed in Issue 7 */
