/* optional: limit constants for I/O scheduling */
#include <limits.h>

/* there are no minimum requirements on these constants and they're
   not necessarily defined, so there is nothing to test:

    {POSIX_ALLOC_SIZE_MIN}
    {POSIX_REC_INCR_XFER_SIZE}
    {POSIX_REC_MAX_XFER_SIZE}
    {POSIX_REC_MIN_XFER_SIZE}
    {POSIX_REC_XFER_ALIGN}

*/

int dummy; /* avoid error for empty source file */
